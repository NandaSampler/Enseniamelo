server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false  # este servidor no se registra a sí mismo
    fetch-registry: false        # no necesita obtener otros registros
  server:
    enable-self-preservation: false
  instance:
    hostname: localhost
